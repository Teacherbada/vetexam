"use client";
import Link from "next/link";
import { useEffect,useState } from "react";
type Data={users:{total:number;today:number;week:number};banks:{total:number;public:number;private:number};questions:{total:number};plans:{plan:string;status:string;count:number}[];reports:{status:string;count:number}[];activityTracked:boolean;pdfTracking:boolean;errorTracking:boolean};
export default function AdminDashboard(){const [data,setData]=useState<Data|null>(null),[error,setError]=useState("");useEffect(()=>{fetch("/api/admin/dashboard",{cache:"no-store"}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error);setData(d)}).catch(e=>setError(e instanceof Error?e.message:"載入失敗"))},[]);if(error)return <main className="p-8">{error}</main>;if(!data)return <main className="p-8">載入管理後台…</main>;const Card=({label,value}:{label:string;value:number})=><div className="rounded-xl border bg-white p-4"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold">{value}</p></div>;return <main className="min-h-screen bg-slate-50 p-5 md:p-8"><div className="mx-auto max-w-6xl"><div className="flex items-center justify-between"><div><h1 className="text-3xl font-bold">VetExam Admin</h1><p className="mt-1 text-slate-500">系統總覽</p></div><div className="flex gap-3"><Link href="/admin/feedback" className="rounded-lg border bg-white px-4 py-2 font-semibold">回報中心</Link><Link href="/" className="rounded-lg bg-slate-900 px-4 py-2 font-semibold text-white">回首頁</Link></div></div><h2 className="mt-8 text-lg font-bold">Users</h2><div className="mt-3 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Card label="總使用者" value={data.users.total}/><Card label="今日新增" value={data.users.today}/><Card label="最近 7 天新增" value={data.users.week}/>{data.plans.map(p=><Card key={`${p.plan}-${p.status}`} label={`${p.plan.toUpperCase()} · ${p.status}`} value={p.count}/>)}</div><h2 className="mt-8 text-lg font-bold">Question Banks</h2><div className="mt-3 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Card label="總題庫" value={data.banks.total}/><Card label="公開題庫" value={data.banks.public}/><Card label="私人題庫" value={data.banks.private}/><Card label="總題目" value={data.questions.total}/></div><h2 className="mt-8 text-lg font-bold">System</h2><div className="mt-3 rounded-xl border bg-white p-4 text-sm text-slate-600">Activity tracking：尚未追蹤　·　PDF tracking：尚未追蹤　·　Error tracking：尚未追蹤</div></div></main>}
