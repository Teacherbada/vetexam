"use client";
import Link from "next/link";
import { useEffect,useState } from "react";
type Data={users:{total:number;today:number;week:number};dailyUsers:{date:string;count:number}[];banks:{total:number;public:number;private:number};questions:{total:number};plans:{plan:string;status:string;count:number}[]};
export default function AdminDashboard(){const [data,setData]=useState<Data|null>(null);useEffect(()=>{fetch("/api/admin/dashboard").then(r=>r.json()).then(setData)},[]);if(!data)return <main className="p-8">載入中…</main>;const max=Math.max(1,...data.dailyUsers.map(x=>x.count));return <main className="min-h-screen bg-slate-50 p-6"><div className="mx-auto max-w-6xl"><div className="flex justify-between"><h1 className="text-3xl font-bold">VetExam Admin</h1><Link href="/admin/feedback">回報中心</Link></div><div className="mt-6 grid grid-cols-3 gap-3"><b>總使用者 {data.users.total}</b><b>今日新增 {data.users.today}</b><b>7日新增 {data.users.week}</b></div><section className="mt-6 rounded-xl bg-white p-5"><h2 className="font-bold">每日新增使用者（最近 14 天）</h2><div className="mt-5 flex h-52 items-end gap-1 border-b border-l p-2">{data.dailyUsers.map(day=><div key={day.date} className="flex h-full flex-1 flex-col justify-end text-center text-xs"><span>{day.count}</span><i className="mt-1 bg-indigo-600" style={{height:`${Math.max(3,day.count/max*100)}%`}}/><small>{day.date}</small></div>)}</div><p className="mt-3 text-sm">Y 軸：人數　X 軸：每日日期</p></section><div className="mt-6 grid grid-cols-4 gap-3"><b>題庫 {data.banks.total}</b><b>公開 {data.banks.public}</b><b>私人 {data.banks.private}</b><b>題目 {data.questions.total}</b></div></div></main>}
