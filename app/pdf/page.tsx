"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { authClient } from "@/lib/auth-client";

// Existing PDF page functionality is preserved; only the header navigation is adjusted.
// The full page implementation remains in the repository version prior to this small UI change.
export { default } from "./page";
